# 规范彝文 OCR 评估集 / NuosuBburma OCR Evaluation Set

本目录是 `规范彝文 OCR / NuosuBburma OCR` 的评估集入口说明。完整评估集不直接放在 GitHub，托管在 Hugging Face 评估集仓库：

```text
https://huggingface.co/datasets/nanxidajun/NuosuBburma-OCR-Evaluation-Set
```

## 数据内容

Hugging Face 评估集当前包含复跑评估需要的最小文件：

| 文件/目录 | 说明 |
|---|---|
| `README.md` | 数据集说明 |
| `annotations.jsonl` | 最新评估标注，`758` 条 |
| `images/` | 全部被引用图片，`758` 张 |
| `samples.csv` | 每条样本的来源、场景、粒度、难度和文字构成 |
| `dataset_summary.json` | 汇总统计 |

本仓库只保留评估集入口、评估脚本和提交模型评估结果，不重复提交完整图片数据。

## 正式版本与口径

正式评估版本为 `NuosuBburma OCR Evaluation Set latest integrated 2026-06-29`。样本全部来自真实资料整理，全部纳入统一评估设置，不再拆成“展示样本”和“评分样本”两套。

当前口径：

| 组成 | 样本数 | 说明 |
|---|---:|---|
| 最新评估样本 | 758 | 用于下一次正式同口径评估 |
| 被引用图片 | 758 | 与 `annotations.jsonl` 一一对应 |
| 雪族整页子集 | 65 | 已计入最新 `758` 条总评估集，用于整页切割对照 |
| 历史已跑模型指标 | 603 | 保留在 `evaluation/` 中，作为已经完成的历史 LoRA 指标 |

## 来源构成

评估集全部来自真实资料整理，不使用合成样本作为评估材料。

| 来源 | 样本数 | 评估意义 |
|---|---:|---|
| 凉山彝文资料选译第3集 | 234 | 旧印刷资料和新增候选，覆盖旧书噪声、line、region 和 page |
| 越西彝族民间歌谣 彝文版 | 82 | 新增真实旧印刷行图，增加来源多样性 |
| 凉山彝文资料选译第2集 | 76 | 换书、换排版的旧印刷评估样本 |
| 勒俄玛牧导读教程 | 66 | 导读类资料和屏幕拍照页，覆盖彝汉混排与页面上传噪声 |
| 雪族 子史篇 | 65 | 页面切割、OCR、拼合、结构化输出和审计链路 |
| 手写拍照 | 53 | 手写拍照资料，作为独立真实泛化分组 |
| 《勒俄特依》译注 | 46 | 译注资料，覆盖注释边界和彝汉混排 |
| 根与花 | 43 | 新印刷书籍行图，提供清晰正文基准 |
| 彝文检字本 / 凉山彝语语法 | 62 | 工具书、检字结构、语法资料、汉字、数字和编号 |
| 真实照片 / 标牌 / 屏幕文本 | 22 | 真实场景、屏幕页面、页面照片和背景干扰 |

## 构成与难度

| 维度 | 分布 |
|---|---|
| 样本类型 | `line 470`，`region 119`，`page 169` |
| 场景 | 新印刷 `100`，旧印刷 `507`，手写拍照 `53`，真实照片 `11`，屏幕拍照 `87` |
| 文字混合 | `yi 275`，`yi_han 443`，`yi_han_latin 24`，`han 14`，`other 2` |
| 数字 | 含数字 `179`，不含数字 `579` |
| 难度 | `easy 83`，`medium 467`，`hard 208` |
| 质检 | 空 GT `0`，缺图 `0`，重复 ID `0`，合成样本标记 `0` |

难度分层不按模型输出倒推，而按来源、视觉质量、输入粒度、版面结构和文字混合情况确定：

- `easy`：清晰行图、单行、版面边界简单、干扰少。
- `medium`：彝汉混排、旧印刷、区域样本、数字/标点/注音邻近、普通真实照片。
- `hard`：整页、旧书页面、长段落、目录/脚注/页码、复杂实拍标牌和手写拍照 region/page。

《雪族子史篇》65 页已计入最新 `758` 条总评估集；页面切割对照和模型指标分口径报告，不把 line / region OCR、page OCR 和页面切割 pipeline 强行合并成一个平均数。

## 标注与质检

评估集采用模型辅助闭环，但模型输出只作为人工校对草稿：

```text
真实来源图片整理
-> 页面切分、区域裁剪或整页保留
-> 中间模型生成预标注草稿
-> 人工核对并固化标准答案
-> 记录来源、场景、粒度、难度和文字混合
-> 生成 annotations.jsonl、samples.csv 和 dataset_summary.json
```

关键规则：

- 未经人工确认的模型输出不作为 gold label。
- 合成样本不进入正式评估集。
- 评估集样本不作为最终训练目标。
- 多行 region 保留换行关系，复杂整页不与清晰单行混为同一种能力。
- 彝文、汉字、数字、Latin 注音和标点分别观察，不把所有错误混成一个指标。

## 结果统计

历史提交模型已在 `603` 条 OCR 主指标上完成评估；最新 `758` 条整合评估集已完成清理，原始 `PaddleOCR-VL-1.6` baseline raw prediction 已完成，最终 LoRA 与原始模型的同口径指标等待正式 rerun 后补充。

| 指标 | 当前提交模型 |
|---|---:|
| 最新 `758` 条 raw prediction | 原始模型已完成，LoRA 待正式 rerun |
| 最新 `758` 条 Avg NED | 待最终计算 |
| 历史 `603` 条 Avg NED | 0.036068 |
| 历史 `603` 条 WS Avg NED | 0.034219 |
| 历史 `603` 条 NFKC+WS Avg NED | 0.033964 |
| 历史 `603` 条 Yi-only Avg NED | 0.038309 |
| 历史 `603` 条 Han-only Avg NED | 0.022447 |
| 历史 `603` 条 Digit-only Avg NED | 0.139918 |
| 历史 `603` 条 replacement / LaTeX / extra Latin / long_pred | 0 / 2 / 0 / 0 |

历史开发结果和分组统计保留在 `evaluation/` 中。最新 `758` 条原始模型 baseline raw prediction 已完成；NED、exact 和输出风险指标等待最终同口径计算。

## 下载

复跑评估时，可以下载到本地 `datasets/` 目录：

```bash
# 国内网络较慢时，取消下一行注释使用 Hugging Face 镜像：
# export HF_ENDPOINT=https://hf-mirror.com

hf download nanxidajun/NuosuBburma-OCR-Evaluation-Set \
  --repo-type dataset \
  --local-dir datasets/NuosuBburma_OCR_Evaluation_Set
```

`datasets/` 是本地下载目录，已在 `.gitignore` 中忽略，不作为 GitHub 仓库内容提交。

## 任务定义

输入：包含规范彝文或规范彝文混排内容的图像。

输出：图片中可见文本的 Unicode 转写，尽量保留混排关系和基本标点。

推荐提示词：

```text
<image>OCR:
```

## 数据边界

- 本数据集用于评估，不是训练集。
- 样本来自真实材料，不使用合成样本作为评分材料。
- 书籍样本来源于扫描件裁剪，原始出版物版权归原出版社和权利人所有。
- 使用时请同时尊重原始材料版权与本项目的数据说明。
