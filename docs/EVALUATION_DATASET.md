# 评估集说明

本文档说明评估集来源、分布、标注方式和复查入口，对应赛事评分中的“评估集质量”。

评估集只使用真实来源样本。难度、输入粒度和文字混合方式都单独统计，避免只用干净行图证明模型能力。

## 设计原则

低资源文字识别可以使用合成数据训练，但不能用合成数据证明真实能力。赛事评分对评估集真实性非常敏感，因此本项目把训练数据和评估数据严格区分：

| 原则 | 说明 |
|---|---|
| 真实来源 | 主评估集只使用真实来源样本，合成样本不进入主结果 |
| 全量计入 | 主评估 `603` 条样本全部计入结果；没有“只展示不计分”的样本 |
| 覆盖多场景 | 覆盖新印刷、旧印刷、规范手写、页面照片、实拍/屏幕图、单行图、区域图和整页图 |
| 保留难样本 | 规范手写、多行区域、脚注、数字、彝汉混排不因难度高而过滤 |
| 可复查 | 保留 annotations、图片、逐样本结果和分组统计 |

评估集保留手写、多行、旧书和混排样本。这些样本难度更高，但更接近真实输入。

## 数据托管

完整评估集通过 Hugging Face 评估集仓库发布：

```text
https://huggingface.co/datasets/nanxidajun/NuosuBburma-OCR-Evaluation-Set
```

正式评估集保留复跑评估所需的最小文件：

| 文件/目录 | 说明 |
|---|---|
| `README.md` | 数据集说明 |
| `annotations.jsonl` | 主评估标注，`603` 条 |
| `images/` | 全部被引用图片，`603` 张 |
| `samples.csv` | 每条样本的来源、场景、粒度、难度和文字构成 |
| `dataset_summary.json` | 汇总统计 |

GitHub 仓库不重复上传完整图片数据，只保留评估集入口说明、评估脚本和提交评估结果：

```text
NuosuBburma_OCR_Evaluation_Set/README.md
docs/EVALUATION_DATASET.md
scripts/eval_nuosubburma.py
scripts/analyze_submission_eval.py
evaluation/
```

## 数据分布

| 维度 | 分布 |
|---|---|
| 总量 | `603` 条主评估样本 |
| 样本类型 | 单行图 `515`，区域图 `84`，整页图 `4` |
| 场景 | 新印刷扫描 `309`，旧书扫描 `235`，规范手写 `53`，实拍/屏幕 `6` |
| 难度 | 简单 `172`，中等 `324`，困难 `107` |
| 文字混合 | 彝文主体 `321`，彝汉混排 `265`，含可见拉丁注音 `17` |
| 数字样本 | 含数字 `81`，不含数字 `522` |

这组分布用于检查模型是否能跨来源、版式和输入粒度保持稳定。

![Evaluation dataset composition](figures/dataset_composition.svg)

## 来源构成

| 来源 | 样本数 | 评估意义 |
|---|---:|---|
| 勒俄玛牧导读教程 | 126 | 教程类资料，覆盖教学文本和彝汉混排 |
| 根与花 | 121 | 新印刷正文，提供清晰行图基准 |
| 凉山彝文资料选译第2集 | 106 | 旧印刷资料，覆盖旧书噪声和版式变化 |
| 凉山彝文资料选译第3集 | 84 | 换书、换排版的旧印刷评估样本 |
| 真实手写 | 53 | 规范手写资料，作为独立真实泛化分组 |
| 《勒俄特依》译注 | 45 | 译注资料，覆盖注释边界和彝汉混排 |
| 彝文检字本 | 32 | 工具书结构、检字和字形边界 |
| 凉山彝语语法 | 30 | 语法资料，覆盖汉字、数字和编号 |
| 真实照片 | 3 | 真实场景图像、背景干扰和可见文本 |
| 屏幕页面 | 3 | 屏幕页面、页面图像和上传噪声 |

## 构建流程

评估集采用“模型预标注 + 人工核对”的方式制作。预标注只作为草稿，最终标准答案以人工核对为准。

```text
真实来源图片整理
-> 页面切割或区域裁剪
-> 中间模型做预标注
-> 人工逐条核对标准答案
-> 删除坏样本
-> 统一基本格式
-> 生成 annotations.jsonl
```

标注格式使用 PaddleOCR-VL messages JSONL：

```json
{
  "id": "gen_yu_hua_line_000001",
  "images": ["images/gen_yu_hua_line_000001.png"],
  "messages": [
    {"role": "user", "content": "<image>OCR:"},
    {"role": "assistant", "content": "ground truth text"}
  ],
  "meta": {
    "source_name": "根与花",
    "source_code": "gen_yu_hua",
    "sample_type": "line",
    "script_mix": "yi",
    "difficulty": "easy"
  }
}
```

## 评估集检查目标

| 检查项 | 目的 |
|---|---|
| 换一本书 | 判断模型是否只适应单一排版书籍 |
| 换一种版式 | 判断旧印刷、工具书、语法书、混排资料是否稳定 |
| 彝汉混排 | 检查汉语、符号、脚注和注音附近是否出现异常输出 |
| 规范手写/照片 | 检查模型在非扫描资料上的表现 |
| 页面照片/上传图 | 检查页面切割和识别过程中哪里容易出错 |
| 单行/多行/整页 | 检验区域识别、阅读顺序和换行边界 |

## 评价指标

主指标：

| 指标 | 含义 |
|---|---|
| 归一化编辑距离（NED） | 越低越好 |
| 空白处理后的平均编辑距离 | 忽略空白差异后的 NED |
| 规范化并忽略空白后的平均编辑距离 | Unicode 规范化和空白处理后的 NED |

辅助诊断指标：

| 指标 | 目的 |
|---|---|
| 只看彝文字符的编辑距离 | 单独观察彝文识别能力 |
| 只看汉字的编辑距离 | 单独观察汉字混排识别能力 |
| 只看数字的编辑距离 | 单独观察数字识别和格式稳定性 |
| 替换符 | 检查是否出现替换符异常输出 |
| 公式化片段 | 检查脚注/符号是否被错误公式化 |
| 拉丁字母 | 统计预测中出现拉丁字母的样本数，需要与人工标注对照 |
| 多余拉丁字母 | 检查人工标注无拉丁字母但预测多出拉丁字母的样本数 |
| 异常长输出 | 检查是否出现异常长输出 |

## 最终评估结果

最终提交模型已在 `603` 条主评估样本上完成评估。原始模型、第一阶段、第二阶段和最终模型结果将在最终评估集冻结后按同一脚本补充。结果表同时给出编辑距离分组和输出风险指标。

| 指标 | PaddleOCR-VL 原始模型 | 当前提交模型 |
|---|---:|---:|
| 样本数 | 最终评估后补充 | 603 |
| 平均归一化编辑距离（Avg NED，越低越好） | 最终评估后补充 | 0.036068 |
| 完全匹配率 | 最终评估后补充 | 67.99% |
| 忽略空白后的平均编辑距离 | 最终评估后补充 | 0.034219 |
| 规范化并忽略空白后的平均编辑距离 | 最终评估后补充 | 0.033964 |
| 只看彝文字符的平均编辑距离 | 最终评估后补充 | 0.038309 |
| 只看彝文字符的完全匹配率 | 最终评估后补充 | 74.96% |
| 只看汉字的平均编辑距离 | 最终评估后补充 | 0.022447 |
| 只看汉字的完全匹配率 | 最终评估后补充 | 93.99% |
| 只看数字的平均编辑距离 | 最终评估后补充 | 0.139918 |
| 只看数字的完全匹配率 | 最终评估后补充 | 85.19% |
| 替换符 / 公式化片段 / 多余拉丁字母 / 异常长输出 | 最终评估后补充 | 0 / 2 / 0 / 0 |

![Evaluation snapshot](figures/evaluation_snapshot.svg)

分组结果保留在以下文件中：

```text
evaluation/README.md
evaluation/charts/
evaluation/tables/by_sample_type.csv
evaluation/tables/by_scene.csv
evaluation/tables/by_difficulty.csv
evaluation/tables/by_script_mix.csv
evaluation/tables/by_source.csv
```

## 结果解读

| 分组 | 结果摘要 | 说明 |
|---|---|---|
| 单行图 | 平均编辑距离 `0.028758` | 清晰行图是当前最稳定输入 |
| 区域图 | 平均编辑距离 `0.079725` | 多行区域更容易出现漏行和边界错误 |
| 整页图 | 平均编辑距离 `0.060449`，仅 `4` 条 | 作为复杂整页诊断，不夸大为完整整页识别能力 |
| 新印刷 PDF | 平均编辑距离 `0.029050` | 新印刷 PDF 效果稳定 |
| 旧印刷 PDF | 平均编辑距离 `0.025771` | 旧印刷书籍在本评估集中表现稳定 |
| 规范手写 | 平均编辑距离 `0.122708` | 规范手写资料与印刷体分开解读，当前作为独立泛化分组观察 |

分组结果显示：印刷体行图较稳，多行区域和规范手写仍是后续提升重点。

## 真实性边界

- 评估集不进入训练包。
- 合成训练样本不进入主评估集。
- 模型预标注只作为人工核对草稿，不直接作为最终标准答案。
- 规范手写、区域图和整页图结果需要与单行图识别分开解读。
- 复杂整页压力测试材料与主评估集分开维护，不混入 `603` 条主结果。
- 评估集中的书籍样本来自扫描件裁剪，原始出版物版权归原出版社和权利人所有。
